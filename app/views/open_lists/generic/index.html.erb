<div class="page-header">
  <h1><%=t '.title', :default => "List of " + @model.display_name.pluralize %></h1>
</div>


<div id="items-list">
  <%= render :partial => 'list' %>
</div>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_item_path,
            :class => 'btn btn-primary' %>

